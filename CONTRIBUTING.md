# 贡献指南

感谢参与 Chemical Engineering RAG / KG。这个项目强调三件事：来源可追溯、内容可直接阅读、审计与生产相互独立。

## 最简单的参与方式

不需要先理解全部内部格式，也不需要生成向量、知识图谱或独立审计报告。

先说明一个实际用途：它能帮助解决什么工程问题，或避免什么误用？讲清核心内容和必要限制即可，不必扩写基础教程。纯导航、重复信息、装饰截图不做 RAG；只为理解其他内容而必要的信息可作关联支撑。为了保留出处而登记的记录，不因此获得检索资格。

1. 新资料先开 [资料入库申请](https://github.com/milklong888/chemical-engineering-rag-kg/issues/new?template=source_submission.yml)，填写题名、版本、页数、哈希、权限和准备处理的范围；已有资料可直接提交 [内容解释或纠错](https://github.com/milklong888/chemical-engineering-rag-kg/issues/new?template=content_contribution.yml) 或 PR。
2. 选一个小范围：一节正文、一条公式、一张图表或一处错误。复制 [单对象提交模板](examples/content-contribution.example.md)，给出来源页、自己的解释、未确定之处和实际改动。
3. 阅读原页及必要上下文，把它讲什么、怎么用、限制是什么说明白即可；不需要面向初学者扩写教程。不要把看不清的符号或未注明的单位猜成事实。
4. 维护者负责将内容转换为正式入库包、补齐关联，并安排另一位审阅者核对来源。材料可以先进入待审状态，只有通过验收才算正式入库。

局部贡献只需声明本次页段和对象；“全书页账本”“整包关系闭合”等要求适用于整合后的正式入库包，不要求每位贡献者重做全书。没有权利公开的原页或截图请勿放进 Issue/PR；证据无法合法核对时，该条内容保持待审。

不熟悉 Git 可以直接填写上面的 Issue 表单。习惯使用 PR 的贡献者，可将填好的模板保存为 `contributions/<source-id>/<topic>.md`（目录按需新建），一个文件处理一个明确的小范围。新资料的正式 ID 由维护者登记，避免多人重复使用同一个编号；不必复制整个私有工作目录。

提交前先按来源 ID、PDF 页码和图表/公式编号搜索已有 Issue 或 PR；同一对象已有投稿时，优先在原投稿下补充或纠错，并说明与旧内容的关系。相同文件不重复登记；不同版本分别说明，不能为了去重合并有差异的原文、数值或条件。

## 投稿后会怎样

| 状态 | 含义 |
|---|---|
| 已收到 / 需补充 | 维护者确认来源、范围和权限；缺少页码、解释或核对方式时，请提交者补充 |
| 待审稿 | 内容已整理为固定版本；生产者自查通过不代表独立验收通过 |
| 局部验收通过 | 另一位审阅者对照原页确认本次对象或修复；仅对声明范围有效 |
| 正式入库 | 整合包的内容、覆盖、关系与检索资格检查通过，并记录所采用的版本 |

文档或工具 PR 合并、内容投稿被保存，都不等于该资料已正式入库。正式入库也不等于已开放检索服务，后续全局索引、向量和发布仍有各自的检查。

不确定内容可以提交，但必须标明具体字段和原因；不得用猜测补齐。若问题影响答案、单位或适用条件，该部分继续待审或排除检索，不能靠一条笼统免责声明视为通过。修改已验收内容时提交新的定点修复版本，保留旧版本与修复依据。

## 先确认可以提交什么

可以公开提交：

- 书目与版本元数据；
- 你有权发布的原创文本、公式转写、图表解释和结构化记录；
- 不含原文载荷的页码、对象 ID、哈希和关系描述；
- schema、校验器、评测代码、测试和文档。

不要公开提交：

- 未获授权的 PDF、扫描页、整页截图或裁图；
- OCR 全文、大段原文、可还原教材的连续内容；
- 本机绝对路径、用户名、聊天目录或临时目录；
- 向量数据库、模型权重、缓存、密钥或访问令牌。

提交者必须确认自己有权提交相关内容。项目维护者可以要求移除无法证明来源或权限的材料。

## 正式入库包流程

下面面向直接提交机器入库包的贡献者和负责整合的维护者。普通内容贡献使用上面的简化入口即可。

1. 先创建“资料提交”Issue，只填写书目、页数、文件哈希、权限状态和计划范围，不上传原文件。
2. 从 `main` 建立独立分支；一个 PR 只处理一个来源、一个章节或一个明确的 scoped repair。
3. 按 [docs/INGESTION_SPEC.md](docs/INGESTION_SPEC.md) 生成贡献包。
4. 运行 schema、页覆盖、ID 唯一、关系闭合、禁用状态和敏感路径检查。
5. 提交 PR，并列出实际变更 allowlist、未改范围和已知限制。
6. 由未参与该批生产的另一位审阅者做 source-detail 独立审计。
7. 只有 blocking findings 为 0 的版本才能进入下一层投影；通过不等于允许生产激活。

## 正式入库包的验收要求

- 来源身份：标题、版本、页数、SHA-256、权限声明。
- 页账本：全书整合包从第 1 页到末页 exactly once，无缺页、无重叠；局部修复声明覆盖范围，并验证未破坏既有全书覆盖。
- 物理容器与语义包分开描述。
- 每个语义包只有一个 `content_class`。
- 每条可检索记录有来源页、证据引用和明确 eligibility。
- 公式、表格、曲线和选型图符合专门解释规则。
- discarded/no-value/evidence-only/supporting 内容不得被误开 embedding。
- KG 只使用粗粒度知识单元；细节留在 RAG/evidence。
- 无 PDF、图片、OCR、向量、数据库、模型或本机路径载荷。
- 所有 activation/production/publish 标志保持 false，除非另有正式发布审批。

## 评审职责分离

生产者可以做自查，但自查不能代替独立审计。独立审阅者必须：

- 回到来源页和邻近正文核对；
- 不只检查生产者列出的候选问题；
- 复算哈希、覆盖、关系和零写入；
- 给出精确 findings 与最小修复范围；
- 不在同一个冻结目录中修补或覆盖结果。

## 提交习惯

- 使用稳定、可读、来源限定的 ID。
- JSON/JSONL 使用 UTF-8；同一类记录保持确定性排序。
- 大型机械变更与语义修复分开提交。
- 不要静默删除重复项；先记录重复关系，再明确保留或 discard。
- commit message 建议使用：`source(scope): concise change`。

## 许可证状态

当前仓库尚未选择开源许可证。提交 PR 表示你确认拥有所提交内容的权利，并允许维护者在本仓库中审阅和保存该贡献；未来若要改用明确许可证，会另行取得所需的贡献者同意。第三方资料本身不因此获得再分发授权。
